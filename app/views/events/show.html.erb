<div>
    <h2><%= @event.title %></h2>
    <small><%= @event.date %></small>
    <p><%= @event.description %></p>
    <h3>Attendees:</h3>
    <ul>
        <% @event.attendees.each do |attendee| %>
            <li><%= attendee.name %></li>
        <% end %>
    </ul>   
</div>
